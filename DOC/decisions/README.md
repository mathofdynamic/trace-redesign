# Architecture Decision Records

This directory stores durable implementation decisions for the TRACE repository. These records describe architecture and engineering choices. Runtime project decisions belong in `.trace/decisions/` after the artifact contract exists.

Create one record per decision. Do not silently rewrite historical decisions. Mark later changes as superseding or amending records.
